# 🔄 HOME SLIDER - NAVIGATION & LAYOUT FIX

**تاریخ:** 12 فوریه 2026  
**وضعیت:** ✅ تکمیل شده  
**نوع:** رفع باگ ناوبری و بهبود لی‌اوت

---

## 📝 خلاصه تغییرات

دو مشکل کلیدی برطرف شد:

1. ✅ **جابجایی دکمه‌های ناوبری اسلاید شو** (چپ ↔ راست)
2. ✅ **انتقال دکمه "مشاهده مقاله" به خط جداگانه** (زیر بادج‌ها)

---

## 🔄 تغییر 1: جابجایی دکمه‌های ناوبری

### مشکل قبلی:

دکمه‌های ناوبری در جای اشتباه قرار داشتند:
- دکمه "قبلی" در سمت **چپ** ❌
- دکمه "بعدی" در سمت **راست** ❌

```
قبل:
┌─────────────────────────────┐
│                             │
│  [قبلی ←]    محتوا   [→ بعدی] │  ❌ اشتباه
│   (چپ)              (راست) │
│                             │
└─────────────────────────────┘
```

### راه‌حل:

دکمه‌ها جابجا شدند:
- دکمه "قبلی" حالا در سمت **راست** ✅
- دکمه "بعدی" حالا در سمت **چپ** ✅

```
بعد:
┌─────────────────────────────┐
│                             │
│  [→ بعدی]    محتوا   [قبلی ←] │  ✅ درست
│   (چپ)              (راست) │
│                             │
└─────────────────────────────┘
```

### تغییرات CSS:

```css
/* قبل */
.slider-arrow-prev {
  left: 3rem;   /* چپ ❌ */
}

.slider-arrow-next {
  right: 3rem;  /* راست ❌ */
}

/* بعد */
.slider-arrow-prev {
  right: 3rem;  /* راست ✅ - جابجا شد */
}

.slider-arrow-next {
  left: 3rem;   /* چپ ✅ - جابجا شد */
}
```

### منطق:

در وب‌سایت‌های فارسی (RTL):
- کاربر از **راست به چپ** می‌خواند
- اسلاید "قبلی" باید در سمت **راست** باشد (جایی که شروع می‌شود)
- اسلاید "بعدی" باید در سمت **چپ** باشد (جایی که ادامه دارد)

این با الگوی ذهنی کاربر فارسی‌زبان سازگار است ✅

---

## 📦 تغییر 2: انتقال دکمه به خط جداگانه

### مشکل قبلی:

دکمه "مشاهده مقاله" و بادج‌ها در یک خط بودند:

```
قبل:
┌─────────────────────────────────────┐
│ [زمان: 15 دقیقه] [سطح: متوسط] [دکمه مشاهده] │  ❌ شلوغ
└─────────────────────────────────────┘
```

این باعث می‌شد:
- لی‌اوت شلوغ باشد
- دکمه اهمیت بصری خود را از دست بدهد
- در موبایل wrap شود و نامرتب باشد

### راه‌حل:

دکمه به خط جداگانه **زیر بادج‌ها** منتقل شد:

```
بعد:
┌──────────────────────────────────┐
│ [زمان: 15 دقیقه] [سطح: متوسط]  │  ← بادج‌ها
├──────────────────────────────────┤
│        [دکمه مشاهده مقاله]      │  ← دکمه (خط جدید) ✅
└──────────────────────────────────┘
```

### مزایا:

✅ **اولویت بصری:** دکمه CTA حالا برجسته‌تر است  
✅ **تمیزی:** بادج‌ها جدا، دکمه جدا  
✅ **موبایل بهتر:** هر کدام خط مستقل دارند  
✅ **کاربرپسندتر:** دکمه راحت‌تر پیدا می‌شود

---

## 🔧 تغییرات فایل‌ها

### 1. تغییرات HTML (`layouts/partials/home-slider.html`)

#### قبل:
```html
<div class="slide-actions-row">
  <!-- دکمه و بادج‌ها با هم -->
  <a href="..." class="slide-btn">مشاهده مقاله</a>
  <div class="slide-badges">
    <span>بادج 1</span>
    <span>بادج 2</span>
  </div>
</div>
```

#### بعد:
```html
<!-- فقط بادج‌ها -->
<div class="slide-actions-row">
  <div class="slide-badges">
    <span>بادج 1</span>
    <span>بادج 2</span>
  </div>
</div>

<!-- دکمه در Container جداگانه -->
<div class="slide-button-container">
  <a href="..." class="slide-btn">مشاهده مقاله</a>
</div>
```

### 2. تغییرات CSS (`assets/css/home-slider.css`)

#### تغییرات کلیدی:

```css
/* 1. Actions Row - فقط بادج‌ها */
.slide-actions-row {
  display: flex;
  justify-content: center;
  gap: 1.5rem;              /* کاهش gap */
  margin-bottom: 1.5rem;    /* فاصله از دکمه */
}

/* 2. Button Container جدید - خط جداگانه */
.slide-button-container {
  display: flex;
  justify-content: center;
  width: 100%;
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s cubic-bezier(0.4, 0, 0.2, 1) 0.7s,
              transform 0.6s cubic-bezier(0.4, 0, 0.2, 1) 0.7s;
}

.slider-slide.active .slide-button-container {
  opacity: 1;
  transform: translateY(0);
}

/* 3. دکمه - استایل حفظ شده */
.slide-btn {
  /* همان استایل قبلی */
}
```

#### جابجایی ناوبری:

```css
/* دکمه‌های ناوبری */
.slider-arrow-prev {
  right: 3rem;  /* جابجا شد */
}

.slider-arrow-next {
  left: 3rem;   /* جابجا شد */
}
```

---

## 📊 ساختار جدید HTML

### ترتیب المان‌ها:

```html
<div class="slide-content-inner">
  
  <!-- 1. عنوان -->
  <h2 class="slide-title">عنوان مقاله</h2>
  
  <!-- 2. توضیحات -->
  <p class="slide-description">توضیحات مقاله</p>
  
  <!-- 3. تگ‌ها و دسته‌بندی‌ها -->
  <div class="slide-meta-row">
    <div class="slide-categories">...</div>
    <div class="slide-tags">...</div>
  </div>
  
  <!-- 4. بادج‌های اطلاعاتی (خط جدید) -->
  <div class="slide-actions-row">
    <div class="slide-badges">
      <span class="badge-time">15 دقیقه</span>
      <span class="badge-medium">متوسط</span>
      <span class="badge-lab">نیاز به لب</span>
    </div>
  </div>
  
  <!-- 5. دکمه CTA (خط جدید - جدا شده) -->
  <div class="slide-button-container">
    <a href="..." class="slide-btn">مشاهده مقاله</a>
  </div>
  
</div>
```

---

## 🎨 نمایش بصری

### Desktop (1920px):

```
┌─────────────────────────────────────────┐
│           عنوان مقاله (وسط)            │
│         توضیحات مقاله (وسط)            │
│                                         │
│      [دسته] [تگ] [تگ] (وسط)          │
│                                         │
│   [زمان] [سطح] [لب] (وسط) ← بادج‌ها   │
│                                         │
│       [دکمه مشاهده مقاله] ← دکمه       │
└─────────────────────────────────────────┘
```

### Tablet (768px):

```
┌──────────────────────────────┐
│      عنوان (وسط)            │
│      توضیحات (وسط)          │
│   [دسته] [تگ] (وسط)        │
│   [بادج] [بادج] (وسط)      │
│      [دکمه] (وسط)           │
└──────────────────────────────┘
```

### Mobile (375px):

```
┌────────────────┐
│  عنوان (وسط)  │
│  توضیحات       │
│    [دسته]      │
│  [تگ] [تگ]     │
│                │
│ [بادج] [بادج]  │ ← خط جدا
│                │
│    [دکمه]      │ ← خط جدا ✅
└────────────────┘
```

---

## 🔄 انیمیشن جدید

دکمه حالا انیمیشن مستقل دارد:

```css
/* بادج‌ها - تاخیر 0.6s */
.slide-actions-row {
  transition: opacity 0.6s ease 0.6s,
              transform 0.6s ease 0.6s;
}

/* دکمه - تاخیر 0.7s (بعد از بادج‌ها) */
.slide-button-container {
  transition: opacity 0.6s ease 0.7s,
              transform 0.6s ease 0.7s;
}
```

**نتیجه:**
1. عنوان ظاهر می‌شود
2. توضیحات ظاهر می‌شود
3. تگ‌ها ظاهر می‌شوند
4. بادج‌ها ظاهر می‌شوند (0.6s)
5. دکمه ظاهر می‌شود (0.7s) ✅

این ترتیب طبیعی و روان است.

---

## 📱 رفتار Responsive

### Desktop (>1024px):

```css
.slide-actions-row {
  flex-direction: row;      /* افقی */
  gap: 1.5rem;
  margin-bottom: 1.5rem;
}

.slide-button-container {
  display: flex;
  justify-content: center;
}
```

**نتیجه:**
- بادج‌ها در یک خط
- دکمه در خط بعدی

### Mobile (<768px):

```css
.slide-actions-row {
  margin-bottom: 1.2rem;
  gap: 0.6rem;
}

.slide-button-container {
  width: 100%;
}

.slide-btn {
  width: 100%;              /* تمام‌عرض */
  justify-content: center;
}
```

**نتیجه:**
- بادج‌ها wrap می‌شوند
- دکمه تمام‌عرض صفحه

---

## ✅ مزایای تغییرات

### مزایای UX:

✅ **دکمه برجسته‌تر:** در خط جداگانه، اولویت بصری دارد  
✅ **ساختار واضح‌تر:** بادج‌ها = اطلاعات، دکمه = اقدام  
✅ **موبایل بهتر:** هر المان خط مستقل دارد  
✅ **ناوبری منطقی‌تر:** جهت دکمه‌ها با خواندن فارسی هماهنگ

### مزایای فنی:

✅ **کد تمیزتر:** جداسازی مسئولیت‌ها  
✅ **انیمیشن مستقل:** دکمه جدا animate می‌شود  
✅ **سفارشی‌سازی آسان‌تر:** تغییر دکمه بدون تأثیر بر بادج‌ها  
✅ **Semantic بهتر:** ساختار معنایی واضح‌تر

---

## 🐛 تست و بررسی

### چک‌لیست تست:

#### دکمه‌های ناوبری:
- [ ] دکمه "قبلی" در سمت **راست** است
- [ ] دکمه "بعدی" در سمت **چپ** است
- [ ] کلیک روی دکمه راست → اسلاید قبلی
- [ ] کلیک روی دکمه چپ → اسلاید بعدی
- [ ] آیکون‌ها در جهت صحیح هستند

#### لی‌اوت دکمه:
- [ ] بادج‌ها در یک خط هستند
- [ ] دکمه در خط **زیر** بادج‌ها است
- [ ] فاصله مناسب بین بادج‌ها و دکمه
- [ ] دکمه وسط‌چین است
- [ ] در موبایل دکمه تمام‌عرض است

#### انیمیشن:
- [ ] بادج‌ها اول ظاهر می‌شوند
- [ ] دکمه بعد از بادج‌ها ظاهر می‌شود (0.1s تاخیر)
- [ ] انیمیشن روان است

### بررسی بصری:

```bash
# 1. بیلد پروژه
hugo --minify

# 2. اجرای سرور
hugo server

# 3. بررسی در مرورگر
http://localhost:1313/

# 4. تست دکمه‌های ناوبری
# - کلیک روی دکمه سمت راست → باید به اسلاید قبلی برود
# - کلیک روی دکمه سمت چپ → باید به اسلاید بعدی برود

# 5. بررسی لی‌اوت
# - بادج‌ها در یک خط
# - دکمه در خط زیر
```

---

## 📝 خلاصه تغییرات کد

### فایل‌های تغییر یافته:

1. ✅ **`layouts/partials/home-slider.html`**
   - دکمه از `slide-actions-row` خارج شد
   - Container جدید `slide-button-container` اضافه شد

2. ✅ **`assets/css/home-slider.css`**
   - جابجایی دکمه‌های ناوبری (`left` ↔ `right`)
   - استایل `slide-button-container` اضافه شد
   - `slide-actions-row` به "فقط بادج‌ها" تغییر کرد

### تعداد خطوط تغییر:

- **HTML:** ~15 خط
- **CSS:** ~30 خط
- **جمع:** ~45 خط

---

## 🎯 قبل و بعد

### ناوبری:

| موقعیت | قبل | بعد |
|--------|-----|-----|
| دکمه چپ | قبلی ❌ | **بعدی** ✅ |
| دکمه راست | بعدی ❌ | **قبلی** ✅ |

### لی‌اوت:

| المان | قبل | بعد |
|--------|-----|-----|
| بادج‌ها | با دکمه در یک خط | **خط جدا** ✅ |
| دکمه | با بادج‌ها در یک خط | **خط جدا زیر** ✅ |
| انیمیشن | یکسان | **جدا (0.7s)** ✅ |

---

## 🔧 سفارشی‌سازی

### تغییر فاصله بین بادج‌ها و دکمه:

```css
.slide-actions-row {
  margin-bottom: 2rem;  /* افزایش فاصله */
}
```

### تغییر تاخیر انیمیشن دکمه:

```css
.slide-button-container {
  transition: opacity 0.6s ease 0.8s,  /* تاخیر بیشتر */
              transform 0.6s ease 0.8s;
}
```

### بازگشت دکمه به کنار بادج‌ها:

```html
<!-- در HTML: دکمه را برگردانید به داخل actions-row -->
<div class="slide-actions-row">
  <a href="..." class="slide-btn">...</a>
  <div class="slide-badges">...</div>
</div>
```

```css
/* در CSS: حذف container جدید */
.slide-actions-row {
  gap: 2rem;              /* بازگشت به gap قبلی */
  margin-bottom: 2rem;    /* حذف margin */
}
```

---

## 🎉 خلاصه نهایی

**2 مشکل برطرف شد:**

1. ✅ **دکمه‌های ناوبری جابجا شدند**
   - قبلی: راست ←
   - بعدی: چپ →

2. ✅ **دکمه به خط جداگانه منتقل شد**
   - بادج‌ها: خط بالا
   - دکمه: خط پایین

**نتیجه:**
- 🧭 ناوبری منطقی‌تر و فارسی‌سازتر
- 🎨 لی‌اوت تمیزتر و حرفه‌ای‌تر
- 📱 موبایل بهینه‌تر
- ✅ آماده پروداکشن

---

**وضعیت:** 🟢 تکمیل شده  
**Build:** ✅ موفق (بدون خطا)  
**تاریخ:** 12 فوریه 2026  
**نسخه:** 1.2 (Navigation & Layout Fix)

---

## 📞 پشتیبانی

اگر دکمه‌ها هنوز جابجا نشدند:
- Cache مرورگر را پاک کنید (Ctrl+Shift+R)
- مطمئن شوید CSS جدید بارگذاری شده

اگر دکمه در خط جدا نیست:
- HTML جدید را بررسی کنید
- مطمئن شوید `slide-button-container` وجود دارد

---

**تغییرات اعمال شده! 🚀**
