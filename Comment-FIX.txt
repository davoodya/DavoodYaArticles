# ROLE
You are a Senior Backend Engineer + Secure PHP Developer + Hugo Static Site Integration Specialist.
Your responsibility is to debug, fix, and harden a JSON-based comment system used in a Hugo static blog.
You must fully investigate the JSON write failure and deliver a production-safe, race-condition-safe, secure implementation.
You are not allowed to produce partial fixes. The system must work reliably.

# PROJECT CONTEXT
Project Type: Hugo Static Blog (Markdown Articles)
Frontend: Static HTML generated by Hugo
Backend: PHP (for comment submission + admin panel)
Comment Storage File: /data/user_comments.json
Admin Panel: /api/admin.php
Comment Submission Endpoint: (likely /api/comment.php or similar – verify)
Hosting: Linux-based shared hosting with PHP enabled

# CURRENT COMMENT LOGIC (ALREADY IMPLEMENTED but not Cpmplete)
- Comments are saved inside: /data/user_comments.json
- JSON structure includes:
  - article_slug
  - name
  - email
  - website
  - comment
  - datetime
  - confirm (boolean)
- Default:
  confirm = false
- Exception:
  If email == "davoodya40@gmail.com"
  then confirm must automatically be true
- Admin panel can toggle confirm state
- Only confirmed comments should render on article pages

# NEW PROBLEM
When user clicks "ثبت دیدگاه":
- JSON write fails
- Comment is not stored
- Error is thrown
- No persistence happens

This must be fully fixed.

--------------------------------------------------

# YOUR TASKS

## 1️⃣ ROOT CAUSE ANALYSIS (MANDATORY)
You must check and verify:

- File path correctness (absolute vs relative path)
- Whether /data directory exists
- Whether file exists
- File permissions (must be writable by PHP user)
- JSON encoding errors
- Race condition during file write
- Missing file locking
- Invalid JSON structure corruption
- PHP error suppression issues
- Incorrect header / response type
- CSRF or method mismatch (POST vs GET)
- Incorrect Content-Type

You must NOT guess.
You must implement defensive-safe code.

--------------------------------------------------

## 2️⃣ IMPLEMENT A SAFE JSON WRITE SYSTEM

You MUST:

- Create file automatically if not exists
- Initialize empty array if JSON invalid
- Use file locking (flock)
- Prevent race conditions
- Validate input
- Sanitize input (XSS prevention)
- Use proper JSON_UNESCAPED_UNICODE
- Handle UTF-8 safely
- Return structured JSON response to frontend

Use this safe writing pattern:

- Open file with fopen
- LOCK_EX
- Read content
- Decode safely
- Append comment
- Rewind
- Truncate
- Write encoded JSON
- fflush
- Unlock
- Close

If file cannot be written → return HTTP 500 with JSON error.

--------------------------------------------------

## 3️⃣ FIX STRUCTURE ISSUES

Fix spelling inconsistency:
If key currently is "confrim"
You must standardize to:
    confirm

And update admin panel accordingly.

--------------------------------------------------

## 4️⃣ INPUT VALIDATION RULES

- name: required, max 100 chars
- email: required, valid format
- website: optional, valid URL
- comment: required, max 2000 chars
- article_slug: required

Reject invalid input with HTTP 400.

--------------------------------------------------

## 5️⃣ SECURITY HARDENING

- Escape output on frontend
- Strip HTML tags from comment
- Prevent script injection
- Add basic anti-spam (hidden honeypot field)
- Block direct file access to /data/user_comments.json via .htaccess

Provide .htaccess rule to deny public access.

--------------------------------------------------

## 6️⃣ ADMIN AUTO-APPROVAL LOGIC

If:
    email === "davoodya40@gmail.com"
Then:
    confirm = true
Else:
    confirm = false

Strict comparison.

--------------------------------------------------

## 7️⃣ FRONTEND REQUIREMENTS

Ensure:

- Form uses method="POST"
- Correct endpoint URL
- Fetch/AJAX properly handles JSON response
- Display success or error message dynamically
- No page reload required
- Prevent duplicate submissions

--------------------------------------------------

## 8️⃣ OUTPUT REQUIREMENTS

You must deliver:

1. Corrected comment submission PHP file (complete)
2. Updated admin.php if needed
3. Sample valid JSON structure
4. Example frontend fetch code
5. .htaccess protection
6. Explanation of what caused the original failure
7. Final checklist to confirm system works

--------------------------------------------------

# DEFINITION OF DONE

The task is complete only when:

✔ Comments are saved successfully
✔ No JSON corruption possible
✔ Concurrent submissions do not break file
✔ Admin approval works
✔ Confirmed comments render correctly
✔ Invalid input rejected
✔ No PHP warnings
✔ No race condition
✔ JSON file always valid
✔ System works after multiple submissions
✔ Works on Linux shared hosting

If any of the above fails → task is NOT complete.

--------------------------------------------------

# CONSTRAINTS

- Do NOT use database
- Do NOT change Hugo architecture
- Must remain JSON-based
- Must be production-ready
- Code must be clean and secure

--------------------------------------------------

Now debug, fix, harden, and deliver a complete working implementation.
No placeholders.
No assumptions.
No partial work.
Only final stable system.
